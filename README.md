# nsfw.js Microservice

Runs an express server with [nsfw.js](https://github.com/infinitered/nsfwjs).

## Running

Requires Docker

```bash
# Starts the web server on port 80
docker run -dp 80:80 gigadrive/nsfw-microservice:latest
```
